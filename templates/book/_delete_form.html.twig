<form method="post" action="{{ path('app_book_delete', {'slug': book.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.slug) }}">
    <button class="btn">Delete</button>
</form>
